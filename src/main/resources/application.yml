spring:
  application:
    name: spring-identity-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/identity_service?useSSL=false&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: PhuongTay110903@
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
        ddl-auto: update
